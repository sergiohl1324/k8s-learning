apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - pdb.yaml
  - rbac.yaml
  - network-policy.yaml
  - external-secret.yaml

commonLabels:
  managed-by: kustomize
  app.kubernetes.io/part-of: k8s-learning
